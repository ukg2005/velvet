{
  "name": "velvet-monorepo",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend-svelte run dev",
    "build": "npm --prefix frontend-svelte run build",
    "start": "npm --prefix frontend-svelte run start",
    "lint": "npm --prefix frontend-svelte run check"
  },
  "dependencies": {
    "is-retry-allowed": "^3.0.0"
  }
}
